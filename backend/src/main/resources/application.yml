spring:
  application:
    name: whoareyou

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/who_are_you?useSSL=false&useUnicode=true&serverTimezone=Asia/Seoul
    username: developer
    password: P!ssw0rd

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true

server:
  port: 4040

secret-key: ThisStatementIsJwtSecretKeyDoNotUseThisStatement
